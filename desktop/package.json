{
  "name": "corpus-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Corpus ローカルアプリ — 純クライアント Electron shell",
  "main": "out/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "predev": "npm run build",
    "dev": "electron .",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^33.0.0",
    "typescript": "^5.6.0"
  }
}
